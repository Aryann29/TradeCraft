import PropTypes from 'prop-types';

const MainBar = (props) => {
  return (
    <div style={props.style}>
MainBar
MainBar    </div>
  )
}
MainBar.propTypes = {
    style: PropTypes.object,
};
export default MainBar
